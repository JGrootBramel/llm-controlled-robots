# Copy to .env and set your keys.
# Used by docker-compose for the remote service (ROSA/LLM).
#
# ROS_MASTER_URI and ROS_IP are set by docker-compose for the remote
# service (remote container talks to robot container at http://robot:11311).

OPENAI_API_KEY=your_openai_key_here
